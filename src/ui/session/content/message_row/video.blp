using Gtk 4.0;

template $PaplMessageVideo : $PaplMessageBase {
  layout-manager: BinLayout {};

  $PaplMessageBubble message_bubble {
    styles ["media"]

    prefix: $PaplMediaVideoTile video_tile {};
  }
}
